import styled from "styled-components";

export const Row = styled.section`
    display: flex;
    justify-content: flex-start;
    flex-wrap: wrap;
    margin-bottom: 30px;
    header{

    }
    
`;